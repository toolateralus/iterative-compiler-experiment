fn main() @entry {

}

fn printf(String, ...) @extern;

#// returns, return types.
fn add_int(i32 a, i32 b) i32 {
  return a + b;
}

#// struct type.
type Vector_2 (
  i32 x, i32 y,
  i32 z, i32 w
) @struct;

#// Tagged union type.
type Error<Okay, Error> (
  Ok(Okay),
  Err(Error),
) @union;

#// Enum type. C like.
type FileError ( 
  FileNotFound,
  BadPermission,
) @enum;
