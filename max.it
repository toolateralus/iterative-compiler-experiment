type Vector_2(i32 x, i32 y, i32 z, i32 w);

fn printf(String, ...) @extern;

fn main() @entry {
  Vector_2 vector;
  i32 n = 225;
  printf("n = %d, vector.x = %d, vector.y = %d\n", n, vector.x, vector.y);
}
