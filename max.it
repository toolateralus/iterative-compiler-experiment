type Vector_2(i32 x, i32 y);

fn printf(String, ...) @extern;

fn main() @entry {
  Vector_2 vector;
  vector.x = 100;
  vector.y = 200;
  i32 n = 225;
  printf("n = %d, vector.x = %d, vector.y = %d\n", n, vector.x, vector.y);
}
